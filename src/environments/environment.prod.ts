//En este archivo se exporta nuestra API de node-api-tracks-master. Este archivo actua bajo el contexto de producción. Es decir, se estaría trabajando el el proyecto real.
export const environment = {
    production: true,
    api: 'http://localhost:3001/api/1.0'
}